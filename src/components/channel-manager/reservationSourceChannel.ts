/** Maps reservation `source` label (list / template) to `channels` config id. */
export const RESERVATION_SOURCE_TO_CHANNEL_ID: Record<string, string> = {
  Airbnb: 'airbnb',
  'Booking.com': 'booking',
  Vrbo: 'vrbo',
}
